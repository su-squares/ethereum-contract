{
  "scripts": {
    "clean": "rm -rf ./build",
    "compile": "truffle compile",
    "test": "npm run clean && sh test/confirm-sol-merged.sh && npm run compile && truffle test"
  },
  "devDependencies": {
    "truffle": "^4.1.14"
  }
}
