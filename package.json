{
  "type": "module",
  "scripts": {
    "clean": "rm -rf ./artifacts ./cache",
    "compile": "hardhat build",
    "test": "npm run clean && sh test/confirm-sol-merged.sh && hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.0",
    "hardhat": "^3.0.0"
  }
}